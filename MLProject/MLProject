name: credit-scoring-model

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "credit_scoring_preprocessing/credit_scoring_preprocessed.csv"}
    command: "python modelling.py --data_path {data_path}"
